# 矩阵变换：Python 练习

先运行只读参考实现：

```bash
python transformations.py
```

只编辑 `exercise_transformations.py`，实现二维旋转、矩阵乘向量、矩阵复合、二维行列式和特征值。每完成一项运行：

```bash
python -m unittest -v test_exercise_transformations.py
```

测试描述可观察行为；不要修改测试。需要比较时阅读只读参考文件 `transformations.py`。
