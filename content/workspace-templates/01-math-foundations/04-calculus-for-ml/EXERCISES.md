# 机器学习微积分：Python 练习

先运行只读参考实现：

```bash
python reference_derivatives.py
```

只编辑 `exercise_calculus.py`，依次完成中心差分、二维梯度、梯度下降和二维 Hessian。每完成一项运行：

```bash
python -m unittest -v test_exercise_calculus.py
```

不要修改测试；可阅读只读参考实现 `reference_derivatives.py` 比较行为。
