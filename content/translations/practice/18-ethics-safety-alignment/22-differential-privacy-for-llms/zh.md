---
kind: practice-translation
language: zh-CN
source:
  repository: ai-engineering-from-scratch
  path: phases/18-ethics-safety-alignment/22-differential-privacy-for-llms/docs/en.md
  revision: 39ea8a1c6d0b61f071226eff7ede4d4105fed820
  sha256: 0f05d09ab51d09c553677ace2ec81064232132803ec1dac721d72e8ad1118db4
status: reviewed
---

# 面向 LLM 的差分隐私

> DP-SGD 仍是标准方法——注入噪声的梯度更新提供形式化的（epsilon, delta）保证。但计算、内存和效用开销都很大；参数高效的 DP 微调（LoRA + DP-SGD）是 2025 年的常见配置（ACM 2025）。两类证据存在张力：基于 canary 的成员推断攻击（Duan 等，2024）报告对语言模型的成功有限；训练数据提取（Carlini 等，2021；Nasr 等，2025）却恢复了大量逐字记忆。2025 年 3 月的解决方案（arXiv:2503.06808）指出，差异在于测量对象——插入的 canary 与“最易提取”的数据。新的 canary 设计无需影子模型即可进行基于损失的 MIA，并首次对使用真实数据训练、具有现实 DP 保证的 LLM 进行了有意义的 DP 审计。替代方案包括 PMixED（arXiv:2403.15638）——在推理时对下一个 token 的分布使用专家混合进行私有预测；以及 DP 合成数据生成（Google Research 2024）。新出现的攻击是“通过 LLM 反馈逆转差分隐私”——利用置信度分数泄漏信息。

**类型：** 构建
**语言：** Python（标准库，DP-SGD 注入噪声与 ε-δ 记账演示）
**前置要求：** 第 01 阶段 · 09（信息论）、第 10 阶段 · 01（大模型训练）
**用时：** 约 60 分钟

## 学习目标

- 定义（epsilon, delta）差分隐私，并陈述 DP-SGD 的步骤。
- 解释 2024–2025 年的张力：canary MIA 与训练数据提取呈现了不同图景。
- 描述 PMixED，以及为什么推理时私有预测可以替代 DP 训练。
- 描述“通过 LLM 反馈逆转差分隐私”攻击。

## 问题

LLM 会记忆数据。Carlini 等人 2021 年证明，生产语言模型能够按需逐字复现训练文本。DP 是一种形式化防御：训练模型，使其输出在理论上不受任何单个训练样本影响。2024–2025 年的证据表明，DP-SGD 虽然必要，但部署时采用的 ε 值未必匹配威胁模型。

## 概念

### （ε, δ）差分隐私

随机算法 M 满足（ε, δ）-DP，是指对任意只相差一个样本的两个数据集，以及任意事件 S：

P(M(D) in S) <= e^ε * P(M(D') in S) + δ。

其含义是：输出分布足够接近（由 ε 参数化），因此除去 δ 概率的例外，无法可靠推断任何单个个体的贡献。

### DP-SGD

Abadi 等人，2016 年。标准步骤如下：

1. 采样一个小批次。
2. 计算每个样本的梯度。
3. 将每个样本的梯度裁剪到阈值 C。
4. 对裁剪后的梯度求和，并加入标准差为 σ * C 的高斯噪声。
5. 用带噪声的和更新参数。

隐私成本由记账器跟踪（Moments Accountant、Rényi DP accountant）。LLM 文献报告的 ε 值因威胁模型、数据敏感性和效用目标而差异很大；不存在普遍“安全”的默认 ε。部分 LLM 训练设置中已发表的示例大致为 ε ≈ 1–10，但这只是说明性范围，不是推荐默认值。较低的 ε 通常需要更多噪声，并可能增加效用损失。

### LoRA + DP-SGD

对前沿模型执行完整 DP-SGD 的成本高得难以承受。LoRA 将梯度更新限制在小型适配器上，减少每个样本的梯度存储。LoRA + DP-SGD 是 2025 年的常见配置。DP 保证适用于适配器；基础模型保持不变。

### 2024–2025 年的张力

两条证据线如下：

- **Canary MIA（Duan 等，2024）。** 将唯一的 canary 插入训练数据，测量成员推断攻击者能否识别它们。该研究报告语言模型上的成功有限，暗示 MIA 很难。
- **训练数据提取（Carlini，2021；Nasr 等，2025）。** 用前缀提示模型，测量它能否从训练数据中恢复逐字文本。该研究报告了大量记忆，暗示在相关意义上 MIA 很容易。

2025 年 3 月的解决方案（arXiv:2503.06808）：两者测量的是不同对象。MIA 针对插入的 canary 询问“样本 e 是否在 D 中？”；提取则询问“我能从 D 中恢复什么？”与隐私最相关的是“最易提取”的样本。Canary 未针对可提取性进行优化，因此会低估这一点。

新的 canary 设计支持无需影子模型的基于损失的 MIA，也首次对真实数据上的 LLM 给出具有现实 DP 保证的有意义审计。

### DP 训练的替代方案

- **PMixED（arXiv:2403.15638）。** 在推理时进行私有预测。多个专家分别读取训练数据的一个分片，对下一个 token 的分布进行混合，并在聚合时加入噪声以实现 DP，完全避免 DP 训练。
- **DP 合成数据生成（Google Research 2024）。** 使用 DP-SGD 对 LoRA 进行微调，采样合成数据，再用合成数据训练下游分类器。

两者都以不同的威胁模型为代价，避开完整 DP 训练的效用成本。

### 通过 LLM 反馈逆转差分隐私

2025 年出现了这种攻击：把经过 DP 训练的模型的置信度分数作为预言机，重新识别个人。即使输出不泄漏信息，置信度分布也可能泄漏。

防御措施是不要暴露置信度，或在暴露前截断/量化置信度。这项要求位于（ε, δ）-DP 训练之外。

### 它在第 18 阶段主线中的位置

第 20–21 课讨论偏差与公平性。第 22 课讨论隐私。第 23 课讨论通过水印实现溯源。第 27 课讨论监管中的数据溯源层。

```figure
an-dp-clip-noise
```

## 使用

`code/main.py` 在玩具二元分类数据集上模拟 DP-SGD。你可以扫描噪声倍数 σ 和裁剪范数 C，跟踪（ε, δ）预算及准确率成本。“Canary 攻击”会插入一个独特训练样本，并测量 DP 之前和之后的对数损失检验能否检测到它。

## 交付

本课产出 `outputs/skill-dp-audit.md`。给定一项语言模型部署的 DP 声明，它会审查（ε, δ）值、所用记账器、MIA 评估协议，以及是否评估了置信度暴露向量。

## 练习

1. 运行 `code/main.py`。扫描 σ ∈ {0.5, 1.0, 2.0}，报告（ε, δ）与准确率之间的权衡。找出效用崩溃的点。

2. 实现 canary 插入和对数损失检验。在 σ = 1.0 时测量 DP-SGD 前后的检测率。

3. 阅读 Nasr 等人 2025 年关于训练数据提取的研究。为什么在中等 ε 下提取成功率不会崩溃？这对“以 MIA 作为评估”意味着什么？

4. 设计一个使用 PMixED（arXiv:2403.15638）、完全在推理时运行的部署。PMixED 解决了什么而 DP-SGD 没有解决的威胁模型？

5. 勾勒“通过 LLM 反馈逆转 DP”攻击。设计一个限制置信度分数泄漏的对策，并估算其部署成本。

## 关键术语

| 术语 | 人们怎么说 | 实际含义 |
|------|------------|----------|
| DP | “（ε, δ）差分隐私” | 数据集相邻变化时输出分布保持接近的形式化隐私保证 |
| DP-SGD | “注入噪声的 SGD” | 梯度裁剪并加入高斯噪声的标准 DP 训练方法 |
| LoRA + DP-SGD | “高效私有微调” | 对低秩适配器执行 DP-SGD，2025 年的标准配置 |
| MIA | “成员推断” | 判断某个样本是否出现在训练数据中的攻击 |
| Canary | “插入的水印样本” | 用于测量 DP 泄漏的唯一训练样本 |
| PMixED | “私有推理混合” | 在下一个 token 分布上使用专家混合实现推理时 DP |
| DP Reversal | “置信度泄漏攻击” | 利用模型置信度作为重新识别预言机的攻击 |

## 延伸阅读

- [Abadi et al. — DP-SGD (arXiv:1607.00133)](https://arxiv.org/abs/1607.00133) — 标准 DP 训练算法
- [Carlini et al. — Extracting Training Data (arXiv:2012.07805)](https://arxiv.org/abs/2012.07805) — 经典训练数据提取论文
- [Duan et al. — Canary MIA on LLMs (arXiv:2402.07841, 2024)](https://arxiv.org/abs/2402.07841) — 成功有限的 MIA
- [Kowalczyk et al. — Auditing DP for LLMs (arXiv:2503.06808, March 2025)](https://arxiv.org/abs/2503.06808) — 解决两类证据的张力
- [PMixED (arXiv:2403.15638)](https://arxiv.org/abs/2403.15638) — 推理时私有预测
