# Use an OpenAI-compatible model adapter

The local tutor backend will use a provider-agnostic OpenAI-compatible chat interface configured with a base URL, model name, and optional API key. This supports local and learner-chosen cloud models without embedding provider credentials or coupling the curriculum experience to a single vendor.
