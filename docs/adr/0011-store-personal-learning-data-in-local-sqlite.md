# Store personal learning data in local SQLite

The local-first application will store personal learning data in one SQLite database and offer JSON and Markdown export. It will not introduce accounts, cloud synchronization, or conflict resolution in the first version; this makes persistence reliable while retaining user-controlled backup and portability.
