# Make the first product local-first

The initial product will be a localhost learning application, not a static public site paired with a separate Skill. It will keep curriculum content, learner-owned progress and conversation state, and model-provider configuration under local control, allowing the same interface to use a local model or a learner-supplied API; a public static curriculum can later be derived from the non-personal content layer.
