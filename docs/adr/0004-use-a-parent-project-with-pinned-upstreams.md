# Use a parent project with pinned upstreams

The unified learning product will be a separate parent project. It will consume the practice curriculum and theory knowledge base as independently versioned, read-only upstreams—preferably Git submodules—while owning Chinese translations, theory-card mappings, the site, and the tutor; this keeps attribution and license obligations explicit and makes upstream updates reviewable rather than silently merging sources.
